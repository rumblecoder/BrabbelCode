package com.brabbelcode;

public class Enums {

    public enum MODE {CREATE, SELECT, DELETE, FREE, UNDO, REDO};
    public enum SELECTION {ALL, NONE, LINE};
    public enum DELETION {ALL, SELECTION};
}
